name: MineSkyItems
version: '1.0.0-BETA'
main: net.mineskyitems.MineSkyItems
api-version: 26.1

folia-supported: true

softdepend:
  - MMOCore
authors: [zBrunoC (Bruno C.), Drawn (Giovane D.)]

commands:
  item:
    description: Comando principal para o plugin de criação e edição de itens
    aliases:
      - msit